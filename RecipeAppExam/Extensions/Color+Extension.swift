// Extensions/Color+AppTheme.swift

import SwiftUI

extension Color {
    /// Warm cream page background — replaces systemGroupedBackground throughout the app.
    /// Light: #FFF3EE  Dark: #1C1411
    static let appBackgroundColor = Color("AppBackground")

    /// Warm near-white surface — replaces secondarySystemBackground for cards and strips.
    /// Light: #FFFCFA  Dark: #2A2019
    static let appSurfaceColor = Color("AppSurface")
}
